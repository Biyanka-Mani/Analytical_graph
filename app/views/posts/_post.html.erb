<% if current_user&.is_admin? && controller_name == 'posts' && action_name == 'show' %>
    <%= area_chart post.view_by_day ,colors:["#a00a"] %>
  <% end %>
 
<div id="<%= dom_id post %>">
  <h2><%= post.title %></h2>
  <h4>posted by <%=  post.user.email %></h4>
  <h6><%=post.views%>Views</h6>
<div>
    <%= post.content%>
  
</div>
